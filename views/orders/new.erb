<h1>PLACE AN ORDER FOR TABLE <%=@party.table_number%></h1>
<!-- <h2>SELECT AN ITEM</h2>

<table>
  <th>Food Name</th>
  <th>Cuisine</th>
  <th>Price</th>
  <th>Allergens?</th>
  <th>Food ID</th>
  <% @foods.each do |food| %>
    <tr>
      <td><a href="/foods/<%=food.id%>"><%= food.name %></a></td>
      <td><%= food.cuisine_type %></td>
      <td><%= food.price %></td>
      <td><%= food.allergens %></td>
      <td><%= food.id %></td>
    </tr>
  <% end %>
</table> -->


<form action="/orders" method="post">
  <input type="text" name="order[food_id]" placeholder="food_id" />
  <input type="hidden" name="order[party_id]" value="<%=@party.id%>" />
  <input type="submit">
</form>