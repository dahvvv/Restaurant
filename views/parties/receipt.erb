<h1>Receipt for Table <%=@party.table_number%></h1>

<ol>
  <% @orders.each do |order| %>
    <li>
      <%=Food.where(:id => order.food_id)[0].name%>:  $<%=order.no_charge == true ? 0 : Food.where(:id => order.food_id)[0].priceprint%>
    </li>
  <% end %>
</ol>

TOTAL:  $<%=@total%>
<br>
TIP:  ___________________
<br>
TOTAL:  ___________________
<br>
<em>Suggested Tips:</em>
<br>
<em>25% = $<%=(@total.to_f * 0.25).round(2)%></em>
<br>
<em>20% = $<%=(@total.to_f * 0.2).round(2)%></em>
<br>
<em>15% = $<%=(@total.to_f * 0.15).round(2)%></em>

<br><br>
Look good?
<br><br>
<a href="/receipt.txt" download>Download Receipt</a>

<h3><a href="/">Back Home</a></h3>